models:
    image: tp33/django
    external_links:
      -  project_mysql:db
    volumes:
      - ./:/app
    ports:
      - "8001:8000"
    command: bash -c "python manage.py makemigrations && python manage.py migrate && mod_wsgi-express start-server --working-directory ./ --reload-on-changes /app/tech_consult/wsgi.py"
